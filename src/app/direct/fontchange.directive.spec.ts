import { FontchangeDirective } from './fontchange.directive';

describe('FontchangeDirective', () => {
  it('should create an instance', () => {
    const directive = new FontchangeDirective();
    expect(directive).toBeTruthy();
  });
});
